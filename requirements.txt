# Worker runtime dependencies. Install into a venv:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
requests>=2.32
# Export: writing the multi-tab .xlsx workbook (pure Python, no system libraries).
openpyxl>=3.1
# Timezone database for Python's zoneinfo. macOS/Linux read the OS's copy; Windows
# ships none, so without this the worker's scheduling and the export's local-time
# columns silently fall back to UTC there. Unused (but harmless) on macOS/Linux.
tzdata>=2024.1
